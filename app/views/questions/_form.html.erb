<%= form_with model: question do |form| %>
  <p><%= form.label :user_id %></p>
  <p><%= form.text_field :user_id %></p>
  <p><%= form.label :body, 'Ask your question!' %></p>
  <p><%= form.text_area :body, size: "60x10" %></p>
  <p><%= form.submit %></p>
<% end %>